{
  "compilerOptions": {
    /* Entrada e saída */
    "rootDir": "src",             // código-fonte
    "outDir": "dist",             // para onde vai o JS compilado

    /* Compatibilidade com Node moderno */
    "target": "ES2020",           // recursos modernos de JS
    "module": "CommonJS",         // CommonJS para rodar fácil no Node
    "moduleResolution": "Node",   // como os módulos são resolvidos

    /* Suporte a import/export */
    "esModuleInterop": true,      // permite importar libs antigas com "import express from 'express'"
    "allowSyntheticDefaultImports": true,

    /* Segurança e consistência */
    "strict": true,               // ativa todas as checagens de tipo
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,         // não checa tipos dentro de node_modules

    /* Extras úteis */
    "resolveJsonModule": true     // permite importar arquivos .json
  },
  "include": [
    "src"                         // código principal
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
